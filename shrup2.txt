import turtle
import random

# Set up screen
wn = turtle.Screen()
wn.title("Snake Game")
wn.bgcolor("black")
wn.setup(width=600, height=600)
wn.tracer(0) # Turns off screen updates

# Snake head
head = turtle.Turtle()
head.shape("square")
head.color("red")
head.penup()
head.goto(0,0)
head.direction = "stop"

# Snake food
food = turtle.Turtle()
food.shape("circle")
food.color("blue")
food.penup()
food.goto(0,100)

# Movement functions
def go_up():
    if head.direction != "down":
        head.direction = "up"
# ... (similar functions for go_down, go_left, go_right)

# Main game loop (simplified)
while True:
    wn.update()
    # ... (add logic for snake movement, collision detection, etc.)